# 极简配置微语言设计文档

## 摘要

本语言是为手写优化、自带类型与约束的配置语言，用最少语法规则实现**编写即校验、数据即合同**的开发体验。它原生支持 IDE 智能提示、模板复用、双向转换及全生命周期安全。通过正交化的语义基元，可自由滑动在纯数据描述与纯模型定义之间，并可扩展为数据校验器、IoC 容器驱动及网络数据交换的契约中心。

---

## 1. 基础语法

### 1.1 字段定义核心

整个语言围绕 `name = default` 构建。字段必须有默认值（除模板必填字段外）。

- **简单值**：`key = 值`
- **复合值（对象、数组、模板调用）**：等号可省略，如 `server { port = 8080 }`。
- **存在标记**：裸 `key` 等价于 `key: <?> = exist`。

### 1.2 类型与约束

在字段名后附加 `: <约束列表>` 声明类型和约束。约束列表用尖括号 `<>` 包裹，内置约束包括类型约束（`int`, `str`, `bool`, `float`, `list`, `dict`, `?` 等）和校验函数（`range`, `size`, `each`, `in`, `ip`, `raw` 等）。

**语法糖**：单一类型约束可省略尖括号，即 `field: int = 10` 等价于 `field: <int> = 10`。

**类型是约束的特化**：`int` 本质是名为 `int` 的约束函数，`?` 是存在类型（值域 `exist | null`），`int?` 是可空类型（值域 `int | null`）。

**重要规则**：

- 模板定义中，字段必须带有类型声明（`:` 或 `:<>`），但 `= default` 可选，省略表示“必填字段”。

### 1.3 层级与分隔

- 对象用 `{ }` 表示，缩进仅美化，不具语法意义。
- 数组用 `[ ]` 表示，元素换行或逗号分隔。
- 逗号与换行近乎等价：换行即分隔，同一行内多个元素用逗号分隔，末尾逗号可选。

### 1.4 字符串与注释

- 字符串双引号包裹，JSON 风格转义。
- 注释为 `#` 单行注释。

---

## 2. 模板系统

### 2.1 定义与引用

- 用 `~模板名 { ... }` 定义参数化模板，内部字段必须带类型声明。
- 实例化：`模板名(覆盖参数...)` 或 `字段名 模板名(...)`（省略等号）。
- 参数支持命名（`port=8080`）和位置（按定义顺序）混用，位置参数在前。

模板本质是**语法宏**：实例化时原地展开为完整字段集，展开后与手写无区分。

### 2.2 导入

`!from 路径 import 模板名1, 模板名2` 导入其他文件中的模板。导入在语法分析阶段处理：解析器递归加载依赖文件，提取指定模板并拼接到当前语法树，最终形成一棵包含所有定义的 RawAst。

---

## 3. 文件类型

- `.inft`：模型/模板文件，仅包含 `~模板` 定义和导入语句。
- `.infd`：数据/实例文件，可包含顶层字段定义、模板实例化等。

---

## 4. 解析流水线与错误策略

流水线分四个阶段，各阶段可独立使用：

> 词法分析 → Tokens → 语法分析 → RawAst → 语义分析 → StandardAst → 转换器 → dict/list

- **词法分析**：字符 → token 流。
- **语法分析**：token 流 → RawAst，处理导入拼接，输出不含导入语句的完整语法树。
- **语义分析**：RawAst → StandardAst。按顺序执行：类型检查/推断、约束执行、宏展开、默认值填充。所有字段在此阶段获得完全确定的类型和值。
- **转换器**：StandardAst → 目标数据（纯 dict/list）。

**错误处理**：阶段内尽可能收集所有错误（如多个字段的约束失败），跨阶段快速失败（上游错误阻止下游运行）。

### 双层 AST

- **RawAst**：仅语法合法，保留原始书写形式（宏调用、存在标记缩略等），附带源码位置。
- **StandardAst**：完全展开、校验，无宏、无存在性歧义，携带可选的元数据（类型、约束、默认值来源等）。

---

## 5. 数据类型与存在性语义

基础类型：`int`, `float`, `str`, `bool`, `list`, `dict`, `?`。  
可空特化：`int?`, `str?` 等。

- `?` 类型：值只能为 `exist` 或 `null`。字段 `caching: <?> = exist` 表示存在性开关，`null` 使键在最终数据中不存在。
- `type?`：允许值为 `类型值` 或 `null`，字段始终保留（值为 `null` 时键存在）。

---

## 6. 设计哲学与正交基

语言通过 **正交基** 实现“概念少而功能强”：

| 基元 | 作用 | 依赖关系 |
| - | - | - |
| `name = default` | 声明字段及其默认值 | 无 |
| `: <约束列表>` | 附加类型/约束契约 | 独立可选 |
| `?` 及 `type?` | 存在性/可空性 | 独立可选 |
| `~模板` 及调用 | 复用抽象 | 独立于字段定义 |

这些基元可任意组合，从纯数据 `key = value` 到全契约 `key: <int, range(1,10)> = 5`，谱系自由。

---

## 7. 综合示例

### **模板文件（infra.inft）**

```infd
~Server {
    host: str = "0.0.0.0"
    port: <int, range(1,65535)> = 80
    features {
        caching: ? = exist
        compression: bool = true
    }
    tags: <list, each(str)> = ["web"]
}
```

### **数据文件（app.infd）**

```infd
!from infra import Server

MyApp {
    api Server(port=443, host="api.example.com")
    db Server(port=5432)
    features {
        caching
        compression = false
    }
    tags ["production", "critical"]
}
```

加载后得到完全校验的字典，可直接用于运行时。

---

## 8. 结语

本语言以极简的语法承载了从数据描述到模型定义、从配置加载到通用校验的完整需求。其正交化设计使得工程实现可拆解为独立模块，各司其职；其光谱式使用支持渐进式复杂度，入门无门槛，进阶无天花板。它是配置领域的“可执行合同”，让数据与契约永恒相伴。
